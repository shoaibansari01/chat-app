app.get("/dateWiseConfirmOrderList", (req, res) => {
    const { dateValue } = req.query;
    console.log(dateValue, " newww")

    //const q = "SELECT * FROM orders WHERE status=2 AND deliverydate=?"

    const q = "SELECT c.cid, c.name, c.mobno, c.deliveryaddress, c.city, c.landmark, c.pincode,c.current_location,o.id, o.orderid, o.mealtype, o.vegetable1, o.vegetable2, o.rice, o.bread, o.sweet,o.deliverytime FROM customer c INNER JOIN orders o ON c.cid = o.cid WHERE o.deliverydate = ? AND status=2";

    db.query(q, [dateValue], (err, data) => {
        if (err) {
            console.log(err)
        } else {
            console.log(data)
            res.status(200).send(data)
        }
    })
})